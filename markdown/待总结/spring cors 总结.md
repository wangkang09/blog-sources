```java
// 这是正常的 Cors 
CorsConfiguration
CorsRegistration
CorsBeanDefinitionParser
CorsConfigurationSource
CorsRegistry  //为 path 赋默认的 cors

//这是做 endpoint 时，所要定义的 cors，两个互不相干
CorsEndpointProperties
```

